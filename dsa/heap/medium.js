// Kth largest element in an array [use priority queue]
//make a min heap of max 3 size and in start put the start 3 element in minheap now the smallest will be on top now start comparing the top to the array and start poping in end we will have the largest 3 element in the heap with the smallest at top


// Kth smallest element in an array [use priority queue]

// Sort K sorted array

// Merge M sorted Lists

// Replace each array element by its corresponding rank

// Task Scheduler

// Hands of Straights
