function soln(arr) {
  for (let i = 0; i < arr.length; i++) {
    const cur = arr[i]
        
  }
}

// s1 + s2 + s3 = 0
// s3 = -(s2 + s1)

const arr = [-2,1,-3,4,-1,2,1,-5,4]

console.log(soln(arr));
